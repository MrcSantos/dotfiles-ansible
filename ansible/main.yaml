- hosts: localhost
  vars_prompt:
    - name: choice_upgrade_distro
      prompt: "Do you want to upgrade your distro? [y/N]"
      private: false

  become: true
  roles:
    - base
    - fonts
    - nnn
    - zsh
    - nvim
    - others

#- hosts: localhost
#  become: false
#  roles:
#    - dotfiles

